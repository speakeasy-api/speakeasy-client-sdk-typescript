/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../../lib/primitives.js";
import { safeParse } from "../../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Billing interval (defaults to month)
 */
export const BillingInterval = {
  Month: "month",
  Year: "year",
} as const;
/**
 * Billing interval (defaults to month)
 */
export type BillingInterval = ClosedEnum<typeof BillingInterval>;

export type CreateLanguageCheckoutSessionRequest = {
  /**
   * Billing interval (defaults to month)
   */
  billingInterval?: BillingInterval | undefined;
  /**
   * URL to redirect to if checkout is canceled
   */
  cancelUrl: string;
  /**
   * List of languages to activate
   */
  languages: Array<string>;
  /**
   * URL to redirect to on successful checkout
   */
  successUrl: string;
};

/** @internal */
export const BillingInterval$inboundSchema: z.ZodNativeEnum<
  typeof BillingInterval
> = z.nativeEnum(BillingInterval);
/** @internal */
export const BillingInterval$outboundSchema: z.ZodNativeEnum<
  typeof BillingInterval
> = BillingInterval$inboundSchema;

/** @internal */
export const CreateLanguageCheckoutSessionRequest$inboundSchema: z.ZodType<
  CreateLanguageCheckoutSessionRequest,
  z.ZodTypeDef,
  unknown
> = z.object({
  billing_interval: BillingInterval$inboundSchema.optional(),
  cancel_url: z.string(),
  languages: z.array(z.string()),
  success_url: z.string(),
}).transform((v) => {
  return remap$(v, {
    "billing_interval": "billingInterval",
    "cancel_url": "cancelUrl",
    "success_url": "successUrl",
  });
});
/** @internal */
export type CreateLanguageCheckoutSessionRequest$Outbound = {
  billing_interval?: string | undefined;
  cancel_url: string;
  languages: Array<string>;
  success_url: string;
};

/** @internal */
export const CreateLanguageCheckoutSessionRequest$outboundSchema: z.ZodType<
  CreateLanguageCheckoutSessionRequest$Outbound,
  z.ZodTypeDef,
  CreateLanguageCheckoutSessionRequest
> = z.object({
  billingInterval: BillingInterval$outboundSchema.optional(),
  cancelUrl: z.string(),
  languages: z.array(z.string()),
  successUrl: z.string(),
}).transform((v) => {
  return remap$(v, {
    billingInterval: "billing_interval",
    cancelUrl: "cancel_url",
    successUrl: "success_url",
  });
});

export function createLanguageCheckoutSessionRequestToJSON(
  createLanguageCheckoutSessionRequest: CreateLanguageCheckoutSessionRequest,
): string {
  return JSON.stringify(
    CreateLanguageCheckoutSessionRequest$outboundSchema.parse(
      createLanguageCheckoutSessionRequest,
    ),
  );
}
export function createLanguageCheckoutSessionRequestFromJSON(
  jsonString: string,
): SafeParseResult<CreateLanguageCheckoutSessionRequest, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      CreateLanguageCheckoutSessionRequest$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateLanguageCheckoutSessionRequest' from JSON`,
  );
}
