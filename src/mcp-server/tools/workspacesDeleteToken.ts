/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { workspacesDeleteToken } from "../../funcs/workspacesDeleteToken.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteWorkspaceTokenRequest$inboundSchema,
};

export const tool$workspacesDeleteToken: ToolDefinition<typeof args> = {
  name: "workspaces-delete-token",
  description: `Delete a token for a particular workspace`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workspacesDeleteToken(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
