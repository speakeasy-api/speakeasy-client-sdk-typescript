/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsHandleCheckoutCallback } from "../../funcs/organizationsHandleCheckoutCallback.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.HandleCheckoutCallbackRequest$inboundSchema,
};

export const tool$organizationsHandleCheckoutCallback: ToolDefinition<
  typeof args
> = {
  name: "organizations-handle-checkout-callback",
  description: `Checkout callback

Handles the Stripe checkout success callback. This endpoint is called by Stripe
after a successful checkout, runs reconciliation (idempotent), and redirects
to the original client success URL stored in session metadata.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await organizationsHandleCheckoutCallback(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
