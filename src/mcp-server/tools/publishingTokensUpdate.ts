/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { publishingTokensUpdate } from "../../funcs/publishingTokensUpdate.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdatePublishingTokenExpirationRequest$inboundSchema,
};

export const tool$publishingTokensUpdate: ToolDefinition<typeof args> = {
  name: "publishing-tokens-update",
  description: `Updates the validitity period of a publishing token

Updates the validity period of a particular publishing token.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await publishingTokensUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
