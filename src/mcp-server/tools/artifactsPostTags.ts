/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { artifactsPostTags } from "../../funcs/artifactsPostTags.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostTagsRequest$inboundSchema,
};

export const tool$artifactsPostTags: ToolDefinition<typeof args> = {
  name: "artifacts-post-tags",
  description: `Add tags to an existing revision`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await artifactsPostTags(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
