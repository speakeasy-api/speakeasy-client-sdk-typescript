/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { githubTriggerAction } from "../../funcs/githubTriggerAction.js";
import * as shared from "../../sdk/models/shared/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: shared.GithubTriggerActionRequest$inboundSchema,
};

export const tool$githubTriggerAction: ToolDefinition<typeof args> = {
  name: "github-trigger-action",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await githubTriggerAction(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
