/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { publishingTokensList } from "../../funcs/publishingTokensList.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$publishingTokensList: ToolDefinition = {
  name: "publishing-tokens-list",
  description: `Get publishing tokens for a workspace

Returns a publishing token for the current workspace`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await publishingTokensList(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
