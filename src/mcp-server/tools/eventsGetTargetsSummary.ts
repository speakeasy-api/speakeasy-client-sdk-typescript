/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { eventsGetTargetsSummary } from "../../funcs/eventsGetTargetsSummary.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetWorkspaceTargetsSummaryRequest$inboundSchema,
};

export const tool$eventsGetTargetsSummary: ToolDefinition<typeof args> = {
  name: "events-get-targets-summary",
  description:
    `Load a lean summary of targets for a workspace with pagination. Returns only essential fields needed by the dashboard UI.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await eventsGetTargetsSummary(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
