/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { githubLinkGithub } from "../../funcs/githubLinkGithub.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.LinkGithubAccessRequest$inboundSchema,
};

export const tool$githubLinkGithub: ToolDefinition<typeof args> = {
  name: "github-link-github",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await githubLinkGithub(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
