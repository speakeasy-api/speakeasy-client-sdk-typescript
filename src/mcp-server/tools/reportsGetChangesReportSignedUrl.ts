/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { reportsGetChangesReportSignedUrl } from "../../funcs/reportsGetChangesReportSignedUrl.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetChangesReportSignedUrlRequest$inboundSchema,
};

export const tool$reportsGetChangesReportSignedUrl: ToolDefinition<
  typeof args
> = {
  name: "reports-get-changes-report-signed-url",
  description:
    `Get the signed access url for the change reports for a particular document.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await reportsGetChangesReportSignedUrl(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
