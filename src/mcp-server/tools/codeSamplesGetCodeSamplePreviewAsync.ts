/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { codeSamplesGetCodeSamplePreviewAsync } from "../../funcs/codeSamplesGetCodeSamplePreviewAsync.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCodeSamplePreviewAsyncRequest$inboundSchema,
};

export const tool$codeSamplesGetCodeSamplePreviewAsync: ToolDefinition<
  typeof args
> = {
  name: "code-samples-get-code-sample-preview-async",
  description:
    `Poll for the result of an asynchronous Code Sample preview generation.

Poll for the result of an asynchronous Code Sample preview generation.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await codeSamplesGetCodeSamplePreviewAsync(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
