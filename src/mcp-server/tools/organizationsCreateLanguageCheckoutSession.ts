/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsCreateLanguageCheckoutSession } from "../../funcs/organizationsCreateLanguageCheckoutSession.js";
import * as shared from "../../sdk/models/shared/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: shared.CreateLanguageCheckoutSessionRequest$inboundSchema,
};

export const tool$organizationsCreateLanguageCheckoutSession: ToolDefinition<
  typeof args
> = {
  name: "organizations-create-language-checkout-session",
  description: `Create language checkout session

Creates a Stripe checkout session for per-language billing.
Used when upgrading to business tier with per-language billing model.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await organizationsCreateLanguageCheckoutSession(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
