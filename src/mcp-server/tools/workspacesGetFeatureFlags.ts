/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { workspacesGetFeatureFlags } from "../../funcs/workspacesGetFeatureFlags.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetWorkspaceFeatureFlagsRequest$inboundSchema,
};

export const tool$workspacesGetFeatureFlags: ToolDefinition<typeof args> = {
  name: "workspaces-get-feature-flags",
  description: `Get workspace feature flags`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workspacesGetFeatureFlags(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
