/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsGetBillingEmail } from "../../funcs/organizationsGetBillingEmail.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$organizationsGetBillingEmail: ToolDefinition = {
  name: "organizations-get-billing-email",
  description: `Get billing email for an organization

Returns the billing email and Stripe customer status for the current organization`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await organizationsGetBillingEmail(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
