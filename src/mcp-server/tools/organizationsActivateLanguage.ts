/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsActivateLanguage } from "../../funcs/organizationsActivateLanguage.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ActivateLanguageRequest$inboundSchema,
};

export const tool$organizationsActivateLanguage: ToolDefinition<typeof args> = {
  name: "organizations-activate-language",
  description: `Activate language

Activates a language for billing. If the language was previously deactivated,
this will reactivate it. If the language is new, it may require checkout.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await organizationsActivateLanguage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
