/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsGetLanguages } from "../../funcs/organizationsGetLanguages.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$organizationsGetLanguages: ToolDefinition = {
  name: "organizations-get-languages",
  description: `Get language billing configurations

Returns all language billing configurations for the organization.
Only returns languages with self-serve billing configured.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await organizationsGetLanguages(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
