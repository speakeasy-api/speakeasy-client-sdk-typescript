/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deletePublishingToken } from "../../funcs/deletePublishingToken.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeletePublishingTokenRequest$inboundSchema,
};

export const tool$deletePublishingToken: ToolDefinition<typeof args> = {
  name: "delete-publishing-token",
  description: `Delete a specific publishing token

Delete a particular publishing token.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await deletePublishingToken(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
