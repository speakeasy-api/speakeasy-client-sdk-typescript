/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { publishingTokensCreate } from "../../funcs/publishingTokensCreate.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreatePublishingTokenRequestBody$inboundSchema.optional(),
};

export const tool$publishingTokensCreate: ToolDefinition<typeof args> = {
  name: "publishing-tokens-create",
  description: `Create a publishing token for a workspace

Creates a publishing token for the current workspace`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await publishingTokensCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
