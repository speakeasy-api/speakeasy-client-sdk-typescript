/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { schemaStoreCreateSchemaStoreItem } from "../../funcs/schemaStoreCreateSchemaStoreItem.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateSchemaStoreItemRequestBody$inboundSchema,
};

export const tool$schemaStoreCreateSchemaStoreItem: ToolDefinition<
  typeof args
> = {
  name: "schema-store-create-schema-store-item",
  description: `Create a schema in the schema store`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await schemaStoreCreateSchemaStoreItem(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
