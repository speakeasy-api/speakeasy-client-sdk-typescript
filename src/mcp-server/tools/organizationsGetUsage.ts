/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsGetUsage } from "../../funcs/organizationsGetUsage.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$organizationsGetUsage: ToolDefinition = {
  name: "organizations-get-usage",
  description: `Get billing usage summary for a particular organization

Returns a billing usage summary by target languages for a particular organization`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await organizationsGetUsage(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
