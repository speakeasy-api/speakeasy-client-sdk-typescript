/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { artifactsGetNamespaces } from "../../funcs/artifactsGetNamespaces.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$artifactsGetNamespaces: ToolDefinition = {
  name: "artifacts-get-namespaces",
  description: `Each namespace contains many revisions.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await artifactsGetNamespaces(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
