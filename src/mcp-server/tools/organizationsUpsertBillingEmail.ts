/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsUpsertBillingEmail } from "../../funcs/organizationsUpsertBillingEmail.js";
import * as shared from "../../sdk/models/shared/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: shared.BillingEmailRequest$inboundSchema,
};

export const tool$organizationsUpsertBillingEmail: ToolDefinition<typeof args> =
  {
    name: "organizations-upsert-billing-email",
    description: `Create or update billing email

Creates a Stripe customer if one does not exist, or updates the billing email for an existing Stripe customer`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await organizationsUpsertBillingEmail(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
