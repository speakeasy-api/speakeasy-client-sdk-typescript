/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsGetBillingAddOns } from "../../funcs/organizationsGetBillingAddOns.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$organizationsGetBillingAddOns: ToolDefinition = {
  name: "organizations-get-billing-add-ons",
  description: `Get billing add ons`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await organizationsGetBillingAddOns(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
