/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { reportsUploadReport } from "../../funcs/reportsUploadReport.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UploadReportRequestBody$inboundSchema,
};

export const tool$reportsUploadReport: ToolDefinition<typeof args> = {
  name: "reports-upload-report",
  description: `Upload a report.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await reportsUploadReport(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
