/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { authGetAccess } from "../../funcs/authGetAccess.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetWorkspaceAccessRequest$inboundSchema,
};

export const tool$authGetAccess: ToolDefinition<typeof args> = {
  name: "auth-get-access",
  description: `Get access allowances for a particular workspace

Checks if generation is permitted for a particular run of the CLI`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await authGetAccess(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
