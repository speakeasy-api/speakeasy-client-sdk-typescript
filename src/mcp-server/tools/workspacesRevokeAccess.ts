/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { workspacesRevokeAccess } from "../../funcs/workspacesRevokeAccess.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RevokeUserAccessToWorkspaceRequest$inboundSchema,
};

export const tool$workspacesRevokeAccess: ToolDefinition<typeof args> = {
  name: "workspaces-revoke-access",
  description: `Revoke a user's access to a particular workspace`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workspacesRevokeAccess(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
