/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { suggestSuggestOpenAPI } from "../../funcs/suggestSuggestOpenAPI.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SuggestOpenAPIRequest$inboundSchema,
};

export const tool$suggestSuggestOpenAPI: ToolDefinition<typeof args> = {
  name: "suggest-suggest-open-API",
  description:
    `(DEPRECATED) Generate suggestions for improving an OpenAPI document.

Get suggestions from an LLM model for improving an OpenAPI document.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await suggestSuggestOpenAPI(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
