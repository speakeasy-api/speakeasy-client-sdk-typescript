/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsDeactivateLanguage } from "../../funcs/organizationsDeactivateLanguage.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeactivateLanguageRequest$inboundSchema,
};

export const tool$organizationsDeactivateLanguage: ToolDefinition<typeof args> =
  {
    name: "organizations-deactivate-language",
    description: `Deactivate language

Deactivates a language. Subject to 2-week cooldown between deactivations.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await organizationsDeactivateLanguage(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
