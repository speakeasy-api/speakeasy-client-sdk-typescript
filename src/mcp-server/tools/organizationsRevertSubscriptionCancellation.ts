/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsRevertSubscriptionCancellation } from "../../funcs/organizationsRevertSubscriptionCancellation.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$organizationsRevertSubscriptionCancellation: ToolDefinition =
  {
    name: "organizations-revert-subscription-cancellation",
    description: `Revert subscription cancellation

Reverts a pending subscription cancellation.
The subscription will continue to renew automatically.`,
    tool: async (client, ctx) => {
      const [result, apiCall] =
        await organizationsRevertSubscriptionCancellation(
          client,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
