/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { artifactsListRemoteSources } from "../../funcs/artifactsListRemoteSources.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListRemoteSourcesRequest$inboundSchema,
};

export const tool$artifactsListRemoteSources: ToolDefinition<typeof args> = {
  name: "artifacts-list-remote-sources",
  description: `Get remote sources attached to a particular namespace`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await artifactsListRemoteSources(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
