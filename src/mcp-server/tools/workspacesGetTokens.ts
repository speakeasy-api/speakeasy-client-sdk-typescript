/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { workspacesGetTokens } from "../../funcs/workspacesGetTokens.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetWorkspaceTokensRequest$inboundSchema,
};

export const tool$workspacesGetTokens: ToolDefinition<typeof args> = {
  name: "workspaces-get-tokens",
  description: `Get tokens for a particular workspace`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workspacesGetTokens(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
