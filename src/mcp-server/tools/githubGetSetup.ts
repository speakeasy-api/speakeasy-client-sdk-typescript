/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { githubGetSetup } from "../../funcs/githubGetSetup.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetGithubSetupStateRequest$inboundSchema,
};

export const tool$githubGetSetup: ToolDefinition<typeof args> = {
  name: "github-get-setup",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await githubGetSetup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
