/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { artifactsSetArchived } from "../../funcs/artifactsSetArchived.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ArchiveNamespaceRequest$inboundSchema,
};

export const tool$artifactsSetArchived: ToolDefinition<typeof args> = {
  name: "artifacts-set-archived",
  description: `Set whether a namespace is archived`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await artifactsSetArchived(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
