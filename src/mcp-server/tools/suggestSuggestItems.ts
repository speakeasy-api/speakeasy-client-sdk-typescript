/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { suggestSuggestItems } from "../../funcs/suggestSuggestItems.js";
import * as shared from "../../sdk/models/shared/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: shared.SuggestItemsRequestBody$inboundSchema,
};

export const tool$suggestSuggestItems: ToolDefinition<typeof args> = {
  name: "suggest-suggest-items",
  description: `Generate generic suggestions for a list of items.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await suggestSuggestItems(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
