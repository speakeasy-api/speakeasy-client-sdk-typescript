/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { artifactsPreflight } from "../../funcs/artifactsPreflight.js";
import * as shared from "../../sdk/models/shared/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: shared.PreflightRequest$inboundSchema.optional(),
};

export const tool$artifactsPreflight: ToolDefinition<typeof args> = {
  name: "artifacts-preflight",
  description:
    `Get access token for communicating with OCI distribution endpoints`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await artifactsPreflight(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
