/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksHandleStripeWebhook } from "../../funcs/webhooksHandleStripeWebhook.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.HandleStripeWebhookRequestBody$inboundSchema,
};

export const tool$webhooksHandleStripeWebhook: ToolDefinition<typeof args> = {
  name: "webhooks-handle-stripe-webhook",
  description: `Handle Stripe webhook

Receives and processes Stripe webhook events for subscription management.
This endpoint is called by Stripe and uses webhook signature verification instead of API authentication.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksHandleStripeWebhook(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
