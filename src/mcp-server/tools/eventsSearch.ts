/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { eventsSearch } from "../../funcs/eventsSearch.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SearchWorkspaceEventsRequest$inboundSchema,
};

export const tool$eventsSearch: ToolDefinition<typeof args> = {
  name: "events-search",
  description: `Search events for a particular workspace by any field`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await eventsSearch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
