/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { publishingTokensResolveMetadata } from "../../funcs/publishingTokensResolveMetadata.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPublishingTokenPublicMetadataRequest$inboundSchema,
};

export const tool$publishingTokensResolveMetadata: ToolDefinition<typeof args> =
  {
    name: "publishing-tokens-resolve-metadata",
    description: `Get metadata about the token

Get information about a particular publishing token.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await publishingTokensResolveMetadata(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
