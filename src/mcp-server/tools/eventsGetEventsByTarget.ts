/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { eventsGetEventsByTarget } from "../../funcs/eventsGetEventsByTarget.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetWorkspaceEventsByTargetRequest$inboundSchema,
};

export const tool$eventsGetEventsByTarget: ToolDefinition<typeof args> = {
  name: "events-get-events-by-target",
  description: `Load recent events for a particular workspace`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await eventsGetEventsByTarget(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
