/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { subscriptionsIgnoreSubscriptionNamespace } from "../../funcs/subscriptionsIgnoreSubscriptionNamespace.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.IgnoreSubscriptionNamespaceRequest$inboundSchema,
};

export const tool$subscriptionsIgnoreSubscriptionNamespace: ToolDefinition<
  typeof args
> = {
  name: "subscriptions-ignore-subscription-namespace",
  description: `Ignored a namespace for a subscription`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await subscriptionsIgnoreSubscriptionNamespace(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
