/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { createPublishingToken } from "../../funcs/createPublishingToken.js";
import * as shared from "../../sdk/models/shared/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: shared.PublishingToken$inboundSchema,
};

export const tool$createPublishingToken: ToolDefinition<typeof args> = {
  name: "create-publishing-token",
  description: `Create a publishing token for a workspace

Creates a publishing token for the current workspace`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await createPublishingToken(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
