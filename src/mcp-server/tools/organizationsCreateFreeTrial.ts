/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsCreateFreeTrial } from "../../funcs/organizationsCreateFreeTrial.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$organizationsCreateFreeTrial: ToolDefinition = {
  name: "organizations-create-free-trial",
  description: `Create a free trial for an organization

Creates a free trial for an organization`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await organizationsCreateFreeTrial(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
