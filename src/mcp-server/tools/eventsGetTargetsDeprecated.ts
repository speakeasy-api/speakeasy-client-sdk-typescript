/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { eventsGetTargetsDeprecated } from "../../funcs/eventsGetTargetsDeprecated.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetWorkspaceTargetsDeprecatedRequest$inboundSchema,
};

export const tool$eventsGetTargetsDeprecated: ToolDefinition<typeof args> = {
  name: "events-get-targets-deprecated",
  description: `Load targets for a particular workspace`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await eventsGetTargetsDeprecated(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
