/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { githubCheckPublishingPRs } from "../../funcs/githubCheckPublishingPRs.js";
import * as operations from "../../sdk/models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GithubCheckPublishingPRsRequest$inboundSchema,
};

export const tool$githubCheckPublishingPRs: ToolDefinition<typeof args> = {
  name: "github-check-publishing-P-rs",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await githubCheckPublishingPRs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
