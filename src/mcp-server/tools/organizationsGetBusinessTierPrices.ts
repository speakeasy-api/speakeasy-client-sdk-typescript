/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { organizationsGetBusinessTierPrices } from "../../funcs/organizationsGetBusinessTierPrices.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$organizationsGetBusinessTierPrices: ToolDefinition = {
  name: "organizations-get-business-tier-prices",
  description: `Get business tier prices

Returns both monthly and annual business tier prices from Stripe`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await organizationsGetBusinessTierPrices(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
