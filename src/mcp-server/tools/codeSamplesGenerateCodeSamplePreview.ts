/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { codeSamplesGenerateCodeSamplePreview } from "../../funcs/codeSamplesGenerateCodeSamplePreview.js";
import * as shared from "../../sdk/models/shared/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: shared.CodeSampleSchemaInput$inboundSchema,
};

export const tool$codeSamplesGenerateCodeSamplePreview: ToolDefinition<
  typeof args
> = {
  name: "code-samples-generate-code-sample-preview",
  description:
    `Generate Code Sample previews from a file and configuration parameters.

This endpoint generates Code Sample previews from a file and configuration parameters.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await codeSamplesGenerateCodeSamplePreview(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
